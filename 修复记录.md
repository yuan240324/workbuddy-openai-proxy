# 修复记录

本文件记录一轮代码审查与修复中处理的问题，按类别列出**现象 → 根因 → 修复**。
测试覆盖见 `test/`，运行方式见 README 的「测试」一节。

---

## 一、安全（P0）

### 1.1 控制台会话 token 可被任意网页窃取

**现象**
`/console` 页面无需鉴权即可访问，且响应带 `Access-Control-Allow-Origin: *`；
页面内以明文脚本注入会话 token（`window.__WB_TOKEN__`）。
两者叠加后，用户浏览器中打开的**任意网页**都能 `fetch('http://127.0.0.1:8788/console')`
读到响应体、提取 token，进而调用控制台接口切换模型、删除凭证、停止服务。

**根因**
服务虽只监听 `127.0.0.1`，但「本机服务」对用户浏览器里的任何页面同样可达。
同源策略本是唯一防线，而全局无条件设置的 `ACAO: *` 把它拆掉了。

**修复**
- `/console` 页面与控制台 API 只接受本机来源：`Origin` 为空（同源导航 / curl 等本机工具）
  或为回环地址（`localhost` / `127.x` / `::1`）才放行；
- 校验 `Host`，抵御 DNS rebinding（恶意域名重绑定到 `127.0.0.1`）；
- 控制台相关响应额外返回 `X-Frame-Options: DENY` 与 `Content-Security-Policy: frame-ancestors 'none'`；
- 控制台路径**不再**返回 `ACAO: *`（确认为本机来源时回显具体 Origin 并带 `Vary: Origin`）；
- `/v1/*` 的宽松 CORS **保持不变**，浏览器内的客户端接入不受影响。

### 1.2 `apiKey` 为空时鉴权被整体跳过

**现象**
`if (!cfg.apiKey) return true` —— 空字符串同样命中该判断，
一旦配置被误清空，服务就完全不校验 API Key。

**修复**
显式判断「trim 后是否为空」，并在启动时打印 `WARN` 提示当前不做鉴权。

### 1.3 启动日志泄露完整密钥

**现象**
启动时 `log('API Key：' + cfg.apiKey)` 会把完整密钥写进日志文件与控制台历史。

**修复**
改为掩码输出（`sk-wb-1a2b3c…f9e8`），完整值仍需查看 `config.json`。

---

## 二、健壮性（P1）

### 2.1 上游元数据接口无超时，会永久挂起

**现象**
`fetchModels()` 与 `queryCredit()` 既无 `signal` 也无超时。
上游一旦挂起，`/v1/models`、`/status`、控制台首屏会**永久卡住**，连接也不释放。

**修复**
新增 `fetchWithTimeout()`，并新增配置项 `timeouts.metaMs`（默认 30s）。
关键在于定时器要覆盖到**响应体读取阶段**——上游先发响应头再挂起 body 同样会卡死，
因此由调用方在读完 body 后才释放定时器。

实测：假上游完全挂起时，`/v1/models` 约 1.2s 返回（而非永久阻塞）。

### 2.2 `recordBalance` 的节流逻辑是死代码

**现象**
```js
if (last && last.v === remain) return;                        // 值没变就不记
if (last && now - last.t < 60_000 && last.v === remain) return; // 永不达
```
第二个条件被第一个完全覆盖，是死代码。作者原意应为「60 秒内不重复采样」，
实际写成了「值不变才跳过」，导致余额频繁波动时每次控制台轮询（20s 一次）
都写入一条，`usage.json` 快速涨到 500 条上限。

**修复**
拆成两条独立规则：值不变不记 + 60 秒内不重复采样；并补 `Number.isFinite`
校验（`NaN` / `Infinity` 会污染趋势图）。

### 2.3 路径匹配用裸 `includes` 子串

**现象**
`pathname.includes('/messages')` 会命中任何含该子串的路径
（如 `/v1/mymessagesfoo`），判定结果依赖 if 的书写顺序，较脆弱。

**修复**
改为按 `/` 切分后匹配**完整路径段**，同时保留 TraeWork 那种
`/v1/messages/chat/completions` 的拼接容错（这是注释里明确要支持的场景），
并把判定顺序写进注释：`responses → count_tokens → chat → messages`。

### 2.4 token 刷新失败后不清除失效凭证

**现象**
上游返回 401 / `invalid_grant` 后，本地仍保留旧 token 与 refresh token，
后续每次请求都尝试刷新并失败，日志反复刷屏。

**修复**
区分两类失败：
- **凭证已死**（401/403、`invalid_grant`、明确的失效业务码）→ 清除本地凭证，
  报 `not_logged_in` 并提示重新登录；
- **临时故障**（500、网络异常、响应无法解析）→ **保留凭证**，报 `refresh_failed`。

这个区分很重要：若一律清除，上游抖动一次用户就得重新登录。

### 2.5 启动失败时抛出裸栈

**现象**
端口被占用、`config.json` 语法错误等情况直接抛出原始异常堆栈，难以排查。

**修复**
- `EADDRINUSE` → 提示「可能已有实例在运行」，给出 `node status.mjs` / `node stop.mjs` / 改端口三条出路；
- `EACCES` → 提示权限问题；
- `config.json` 非法 JSON → 给出常见原因（漏逗号、单引号）与校验方法，
  并且**绝不覆盖用户文件**；
- 配置校验问题改为在启动日志中逐条列出。

---

## 三、配置校验与降级

`config.json` 是用户手改的文件，写错类型时原先会让服务以难懂的方式崩掉。

**新增 `validateConfig()`**：类型校验 + 安全降级 + 问题上报。

| 配置写法 | 修复前 | 修复后 |
|---|---|---|
| `"timeouts": null` | 所有对话请求 500（`Cannot read properties of null`） | 回退默认超时，启动日志提示 |
| `"sites": null` | 站点静默消失 | 恢复内置站点预设 |
| `"port": "abc"` / `null` | 监听失败 | 回退 8788 |
| `"stripFields": "x"` | 遍历字符串，行为诡异 | 回退 `[]` |
| `"modelRoutes": {"m":"ghost"}` | 静默路由到默认站点 | 移除坏路由并说明 |
| `"defaultMaxTokens": -1` | 传给上游 | 回退默认值 |
| `"defaultSite"` 指向不存在/已禁用站点 | 必然路由失败 | 改用第一个可用站点 |

**覆盖字段**：`host`、`port`、`apiKey`、`defaultModel`、`defaultSite`、
`defaultMaxTokens`、`defaultSystemPrompt`、`timeouts`、`sites`、`stripFields`、
`modelRoutes`、`modelAliases`、`models`、`allowModels`、`excludeModels`、
`exposeSitePrefixed`、`upstreamRetry`。

**两条边界约定**：
- **合法配置零改动** —— 默认配置跑校验返回空问题列表、对象逐字节不变；
- `sites.<站点>.billingBase` 为**可选**字段 —— 协议不同的站点（如 DeepSeek 官方）
  不使用 CodeBuddy 的计费接口，预设里本就没有该字段，不能因缺失而报问题。

若配置不是合法 JSON，抛出带 `code: 'INVALID_CONFIG_JSON'` 的错误由调用方处理，
**不静默重置用户文件**。

---

## 四、控制台界面

原先的问题：全页只有 1 个背景色 + 2 个面板色（亮度差约 3%），所有卡片糊成一片；
字号存在 `12.3px` / `12.5px` 这种肉眼无差别的混用；圆角有三种不一致取值；
宽屏下内容左对齐、右侧大片空白。

**改动**
- 建立**设计令牌**体系（颜色 / 间距 / 字号 / 圆角），深色与浅色两套主题共用同一组语义变量；
- 深/浅主题一键切换，`localStorage` 记忆，首次访问跟随系统 `prefers-color-scheme`，
  并在 `<head>` 前置脚本中定下主题以避免首屏闪烁；
- 重建视觉层级：底 → 面板 → 次级块 → 凹陷区四级，配合描边与阴影；
- 统一字号为 5 档、间距为 6 档、圆角为 3 档；
- 内容居中（`margin: 0 auto`，`max-width: 1240px`）；
- 导航改为分段控件风格，选中项带圆点指示；
- 表格加容器描边圆角，表头改大写字距。

**顺带修掉的问题**
- 表头 `th { top: 57px }` 与页头实际 64px 不匹配，页面滚动时表头被页头压住 7px；
- 键盘焦点 `outline` 用了正文色，等于没有焦点提示；
- `stopService()` 里硬编码深色配色，浅色主题下会突然变黑；
- 深色下系统默认滚动条刺眼。

**可访问性**：所有文字对比度达到 WCAG AA（≥4.5:1）。
过程中发现浅色主题的次要文字仅 3.62:1、标签仅 3.06:1，均已调整至达标。

---

## 五、错误提示准确性

以下三处都属于同一类问题：**报错内容把排查方向引向错误的地方**。

### 5.1 额度查询把「该站点不适用」误报成网络故障

**现象**
`queryCredit()` 无条件拼接 `siteCfg.billingBase + '/v2/billing/meter/get-user-resource'`。
协议不同的站点（如 DeepSeek 官方走 PoW + `/api/v0/*`）本就没有 `billingBase`，
于是拼出 `undefined/v2/...` 的非法 URL，又被统一报成「30s 超时或网络异常」。

**修复**
- 新增 `supportsCreditQuery(cfg, site)` 判断站点是否支持额度查询；
- `queryCredit()` 对不支持的站点抛出明确原因（501），不做无效请求；
- 新增 `describeFetchFailure()` 区分三类失败：**超时** / **URL 无效（配置缺失）** /
  **网络异常**，不再混为一谈；
- `/status` 与控制台 `/state` 跳过不支持的站点，界面显示「—」而不是「查询失败」。

### 5.2 模型被过滤时指向了错误的配置项

**现象**
`isExcluded()` 同时管 `allowModels` 白名单与 `excludeModels` 黑名单，
但 4 处 404 提示**一律**写成「已在剔除名单里（config.json 的 excludeModels）」。
当真正原因是白名单没命中（`excludeModels` 其实是空的）时，
用户会去改一个完全无关的字段。

**修复**
新增 `explainExcluded()` 返回具体原因，并附上当前白名单/黑名单内容：

```
改前：模型 X 已在 deepseek 站点的剔除名单里（config.json 的 excludeModels）
改后：模型 X 在 deepseek 站点不可用：不在 allowModels 白名单里（当前白名单：glm-*、deepseek-v4*）
```

### 5.3 调用已禁用站点的模型时原因不明

**现象**
`splitPrefix()` 对已禁用站点返回 `null`，于是 `站点/模型` 这种写法会被
**整体当成模型名**继续往下走，最终报成「不在 allowModels 白名单里」——
真实原因其实是站点被禁用了。

**修复**
新增 `disabledSitePrefix()` 识别「站点存在但 `enabled === false`」，
直接说明真实原因并给出启用办法。

**注意保留的区分**：完全**未知**的前缀仍按模型名处理
（模型 ID 本身可能含斜杠，如 `org/model`）；
只有「站点存在但被禁用」才报错。

---

## 六、测试

新增 `package.json` 与 `test/`，使用 Node 内置 `node:test`，**零第三方依赖**，
无需 `npm install`（Node ≥ 18）。

```bash
npm test
# 或：node --test --experimental-test-isolation=none "test/**/*.test.mjs"
```

| 测试文件 | 覆盖内容 |
|---|---|
| `config.test.mjs` | 配置校验：坏值降级、合法配置零改动 |
| `config-load.test.mjs` | 配置加载链路、非法 JSON 处理、旧版配置迁移 |
| `upstream.test.mjs` | 请求体改写、`tool_choice` 归一、SSE 聚合、错误信封识别 |
| `anthropic.test.mjs` | Anthropic ↔ OpenAI 双向转换、`tool_result` 顺序约束 |
| `router.test.mjs` / `router.resolve.test.mjs` | 倍率解析、模型→站点路由优先级 |
| `model-filter.test.mjs` | 模型白/黑名单与错误提示准确性 |
| `credit.test.mjs` | 额度查询的适用性判断 |
| `auth.test.mjs` | 凭证刷新、失效清除与临时故障的区分、JWT 解析、并发单飞 |
| `timeout.test.mjs` | 上游挂起时必须超时返回（含响应体阶段） |
| `paths.test.mjs` | 路径段匹配与 TraeWork 拼接容错 |
| `usage.test.mjs` | 用量统计、余额采样节流 |
| `util.test.mjs` | token 估算、请求体解析与大小上限 |
| `security.test.mjs` | 端到端安全：控制台来源校验、DNS rebinding、鉴权 |

**测试隔离**：通过 `setConfigDir()` / `loadConfig(dir)` 把配置目录指向临时目录，
不会读写仓库里的 `config.json`、`auth.*.json`、`usage.json`。
每个测试文件在用例边界上重新声明自己的前提，因此与执行顺序无关。

> 若运行环境不允许 `--experimental-test-isolation=none`（该参数让测试在单进程内运行），
> 去掉它即可——每个测试文件也会在独立进程里跑，同样通过。

---

## 附：记录到的既有行为（未改动）

以下为审查中发现、但**未修改**的既有实现，列出以免日后误判为新引入的问题：

- `aggregateFrames()` 聚合工具调用时，`function.arguments` 用 `+=` 累加，
  而 `function.name` 用 `=` 赋值（非累加）。主流上游都在首个 chunk 给出完整函数名，
  实际很少触发；现有测试锁定了该语义。
